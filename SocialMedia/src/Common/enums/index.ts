export enum GenderEnum {
  Male,
  Female,
}

export enum ProviderEnum {
  System,
  Google,
}

export enum RoleEnum {
  User = "user",
  Admin = "admin",
}
export enum TokenEnum {
  Access = "access",
  Refresh = "refresh",
}
